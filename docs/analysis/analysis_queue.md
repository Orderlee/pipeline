# 분석 큐 (2026-08-27) — 순차 실행, 완료 즉시 노션 §추가

노션 페이지: 3c96a557-fb8e-81c3-83af-cb4286991b6b (작업 트래커 「프롬프트 뱅크 라벨-free 3분석…」) — §7 부터 이어 씀.
실행 규약: 컨테이너 docker-analysis-1, nohup 분리, COS_THREADS=4, nice 10, 로그 폴링. 한 번에 한 계산 잡. 결과는 CSV/JSON 재계산으로 검증 후 게시.
⚠️ **메모리**: 호스트 62GB 2인 공유, 가용 5~8GB. 뱅크 크기가 547~49,140 문장으로 4배 넘게 벌어져
`7498 × n` 행렬을 한 번에 잡으면 OOM 으로 죽는다(2026-08-27 실측, v1 이 v1.0.12.0 에서 Killed).
프레임을 **행 청크(1,500)** 로 흘리고, 뱅크당 CSV 체크포인트를 남긴다.
⚠️ **뱅크 집합**: `preds.npz` 에는 35종이 있으나 `v2.0.5.*` 4종은 클래스가 `['class_5','normal']` 로
falldown/fire/smoke 가 없어 4클래스 GT 채점이 불가능하다 → 리포트 전역에서 **31종**을 쓴다.
⚠️ **`/embed_text` 계약**: form 필드 `text`, 한 번에 한 문장, 웜 ~7.5ms. 새로 인코딩한 벡터와
저장된 `entity_type='prompt'` 벡터의 cos = **1.00000000** (3/3 실측) → 생성 문장을 같은 공간에서 채점 가능.
데이터 사실: 캡션 임베딩(PG entity_type=caption 11,978, 한국어) 과 frames 임베딩(24현장)은 영상 집합이 서로소. 짝은 **sourcei**(FiftyOne, src_video/t_sec/event_index) ↔ sourcei_site-g 656 + sourcei_v2 52 이벤트 캡션만 가능. EN 캡션 임베딩은 대규모로 없음(FiftyOne frames 264프레임/11캡션만).

| # | 분석 | 상태 | 산출물 |
|---|---|---|---|
| B2 | 클래스별 문장 PCA + 프레임 투영 AUC | **완료·게시 §7** | csv/25*, f28, f29 |
| B1 | 캡션→프레임 검색 Recall@k | **완료·게시 §8** | csv/26*, f30 |
| B3 | CCA/Procrustes 정렬 | **완료·게시 §9** | csv/27*, f31 |
| C4 | 군집 순도/NMI | **완료·게시 §10** | csv/28, f32 |
| C5 | 배치 지원 감사 | **완료·게시 §11** | csv/29, f33 |
| C1+C2 | 대조 친화도 + 프로젝트-잔차 회귀 | **완료·게시 §12** | csv/30*, f34, f35 |
| E2 | PR-AUC/랭킹 지표 (31뱅크 × 점수 3종) | **완료·게시 §13** | csv/31, f36 |
| E3 | 이벤트 단위 인과 집계 (trailing window) | **완료·게시 §14** | csv/32, 32b, f37 |
| E1 | 프루닝 3컷 + **중복 기제(top-K vs argmax)** | **완료·게시 §15** | csv/33, 42, f38, f44 |
| C3+D3 | Biclustering · 문장 방향 산술 | **완료·게시 §16** | csv/34, 35, f39 |
| D1+D2 | 대조쌍/차 벡터 · CuPL 생성 + 라벨-free 필터 + **공정 랭킹 대조** | **완료·게시 §17** | csv/38~41, 43, 44, f42, f43, f45 |
| D4 | 구문 β 재추정 (GT 목표 vs 라벨-free 목표 일치도) | **완료·게시 §18** | csv/36, 37, f40, f41 |
| R1 | 혼합효과 모형 (카메라 랜덤절편, 프로파일 REML 직접 적합) | fable | **완료·게시 §19** | csv/45, 46, f46 |
| R2 | DPP(품질×다양성) vs facility-location — greedy MAP 직접 구현 | fable | **완료·게시 §20** | csv/47, f47 |
| R3 | 불일치 표집 active learning (기준 검증 + 후보 768장) | fable | **완료·게시 §21** | csv/48, 49, f48 |
| D4 | phrase β 재추정 (GT 대조 vs 라벨-free 대조 계수 일치도) | fable | **완료·게시 §18** | csv/36, 37, f40, f41 |
| 완료 | A1 프로토타입 · A2 부분모듈 · A3 허브니스/MI · 주효과/특이도(§3) | 게시됨 | csv/21~24 |

**큐 종료 — 14건 전부 완료·게시(§7~§18).** 산출물 정본은 리포 루트 `sourcei_gt_csv/`
(CSV 65종 + `sourcei_gt_analysis.xlsx` 41시트 + 요약 JSON 18종 + README).

## 이번 큐에서 확정된 것 (한 줄씩)
- **§15** 특이도 하위 25% 컷 = 무위험 절감(488,334→366,239, 0.02 넘게 손해 본 뱅크 0). 주효과 컷은 중앙값 음수. 뱅크 클수록 더 지워도 됨(ρ −0.449). 중복컷 손해의 65%는 **규칙(top-K 가 문장 수를 센다)** 탓
- **§16** 방향 산술은 **전역 문장평균 제거**만 유효(30/31, p 3.0e-8). 동시군집 6개 중 이벤트 팩 3개, 각각 한 현장 가족
- **§17** 규칙으로 만든 **499문장이 12,511문장을 이긴다**(+0.034, CI[+0.007,+0.060]) — 단 정상 오탐 4.3배. 차 벡터는 실패. 분포-IoU 열세는 크기 탓 아님(하향표집 대조)
- **§18** 라벨-free 큐레이션은 fire(ρ +0.46)·smoke(ρ +0.38)만 가능, **falldown 은 ρ −0.02 로 불가** → falldown 약참조가 다음 병목. β 상위는 장소 어휘라 그대로 쓰면 장소 검출기

## 다음에 하면 값이 되는 것 (미착수)
1. **falldown 약참조 만들기** — SAM3 person + 자세 판정 또는 소량 사람 라벨. §18 이 지목한 유일한 병목
2. **문장 정본 1벌** — 31뱅크 488,334 → 고유 121,614. 클래스별 중복 비대칭(falldown 17% vs normal 65%)이 프루닝을 막는 근본 원인
3. **카메라 확충** — §5 의 필요 카메라 65대. 현재 15대로는 뱅크 간 차이가 원리적으로 판정 불가
4. **오탐 예산과 집계 규칙 동시 설계** — §14(이벤트 단위 집계)와 §17(생성 뱅크 오탐 4.3배)을 함께 걸어야 현장 투입 가능


## 큐 종료 (2026-08-28)

제시된 방법론 표의 **모든 항목이 실행·게시**됐다 (노션 §0~§21, 그림 29장, CSV 46시트 XLSX).
의도적으로 하지 않은 것은 표의 "하지 않아야 할 것" 목록 그대로이고, **그 이유를 §22 에 근거와 함께
남겼다** — 특히 phrase β 계수의 통계적 유의성. 핵심은 셋이다:
 (1) 문장이 근접중복이라 유효 표본이 명목의 몇 분의 일이다(falldown 유지율 16.9%) → SE 가 자릿수로 과소평가.
     **§18 의 부호안정성 638/640(99.7%) 도 같은 이유로 부풀려져 있다** — 유의성 대용품이 아니다.
 (2) 종속변수(GT 대조)가 카메라 15대·deff 232·유효표본 32 에서 나온 추정치인데 문장 수준 잔차는 이를 모른다.
 (3) Ridge 는 편향 추정량 + α 를 같은 데이터 CV 로 골라 사후선택 문제까지 겹친다.
그리고 p값 이전에 **falldown 은 두 목표 간 ρ=−0.023·부호일치 49.4%** 로 이미 계수가 안 맞는다.

⚠️ 공유 호스트 예의: R1~R3 은 `COS_THREADS=2` + `nice -n 19` 로 **한 번에 하나씩** 순차 실행했다.
   전량 행렬을 잡지 말고 프레임 1,500행 청크로 흘릴 것(§메모리 주의 참고).

⚠️ `analysis.frame_cluster.entity_id` 는 **pgvector UUID** 이고 FiftyOne 샘플 id(ObjectId)가 아니다.
   FiftyOne `frames` 의 `entity_id` 필드로 조인해야 한다 — `ds.select(UUID)` 는
   `bson.errors.InvalidId` 로 죽는다(2026-08-27 실측).
